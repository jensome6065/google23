extends layout

block content
  h1=title

  form(method='POST' action='')
    div.form-group
      label(for='text') Text:
      input#username.form-control(type='text' placeholder='Text' name='text' required='true' value=(undefined===user ? '' : user.username) )
    button.btn.btn-primary(type='submit') Submit
  if errors
    ul
      for error in errors
        li!= error.msg
